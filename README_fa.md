🌏 Read me in [English](README.md])

# Meaning-Ankidroid: معمار خودکار کارت‌های واژگان

![](Docs/Persian.png)

برنامه Meaning-Ankidroid یک ابزار حرفه‌ای مبتنی بر پایتون است که برای تبدیل لیست ساده‌ای از کلمات به فلش‌کارت‌های غنی و مجهز به چندرسانه‌ای طراحی شده است. با خودکارسازی جمع‌آوری داده‌ها از ۷ دیکشنری ممتاز و منابع رسانه‌ای با کیفیت، این ابزار به زبان‌آموزان اجازه می‌دهد به جای وقت صرف کردن برای ساخت کارت، بر یادگیری تمرکز کنند.

### نحوه استفاده

شروع کار با این برنامه بسیار ساده طراحی شده است:

۱. **آماده‌سازی ورودی**: یک فایل اکسل ایجاد کنید و کلمات هدف خود را در ستون اول (ستون A) بدون ردیف عنوان قرار دهید.
۲. **شخصی‌سازی تجربه کاربری**:

* **زبان ترجمه**: تنظیمات را باز کنید تا زبان مقصد خود را برای گوگل ترنسلیت انتخاب کنید (مانند فارسی، اسپانیایی، فرانسوی و غیره).
* **انتخاب دیکشنری**: از بین ۷ دیکشنری موجود، مواردی را که می‌خواهید داده‌های کارت از آن‌ها استخراج شود، انتخاب کنید.
  ۳. **اجرای اسکریپت**: فایل `main_script(word_by_word).py` را از طریق ترمینال اجرا کنید. سیستم با استفاده از منطق کلمه به کلمه، تعاریف، مثال‌ها و رسانه‌ها را به طور بهینه جمع‌آوری می‌کند.
  ۴. **وارد کردن فوری از طریق .apkg**: علاوه بر فایل‌های .csv و .xlsx، این برنامه فایل **.apkg** نیز تولید می‌کند. این قابلیت اجازه می‌دهد تا با یک کلیک، کارت‌ها را در انکی ویندوز یا انکی‌دروید اندروید وارد کنید، در حالی که تمام استایل‌ها، تصاویر و صداها به درستی حفظ شده‌اند.

### ویژگی‌های کلیدی

* **هفت دیکشنری ممتاز**: این ابزار داده‌ها را از هفت منبع مجزا از جمله فست‌دیکشنری، فرازین، دیکشنری گوگل، کمبریج و Dictionary.com تجمیع می‌کند تا جامع‌ترین تعاریف را ارائه دهد.
* **تلفظ با دو لهجه**: سیستم فایل‌های صوتی با کیفیت بالا را در هر دو لهجه **آمریکایی و بریتانیایی** برای هر کلمه دریافت می‌کند.
* **یادگیری بصری خودکار**: برنامه به طور خودکار تصاویر کلیپ‌آرت مرتبط را برای هر کلمه پیدا می‌کند تا ماندگاری در حافظه بلندمدت بهبود یابد.
* **پشتیبانی از ترجمه جهانی**: یکپارچگی با گوگل ترنسلیت امکان ترجمه کلمات به هر زبان مقصدی را فراهم می‌کند.
* **پردازش قدرتمند**: بهره‌مندی از قابلیت ذخیره خودکار و اجرای موازی (چندرشتگی) برای جلوگیری از فقدان داده و افزایش سرعت پردازش در لیست‌های طولانی.

### معماری سیستم

* **main_script(word_by_word).py**: مدیریت منطق اصلی، پردازش دسته‌ای و تنظیمات اجرای موازی را بر عهده دارد.
* **scraper_functions.py**: شامل کتابخانه تخصصی توابع برای استخراج از سایت‌های خاص و دانلود رسانه‌ها.
* **anki_exporter.py**: مسئول قالب‌بندی داده‌های جمع‌آوری شده در فایل‌های سازگار با انکی و بسته‌های .apkg.

### پیش‌نیازها

* پایتون نسخه 3.x
* مرورگر گوگل کروم و ChromeDriver مطابق با نسخه مرورگر.
* کتابخانه‌های مورد نیاز: pandas، selenium، beautifulsoup4، requests، googletrans، Pillow و python-dotenv.

### سلب مسئولیت

این ابزار صرفاً برای استفاده شخصی و آموزشی در نظر گرفته شده است. از کاربران درخواست می‌شود به شرایط خدمات وب‌سایت‌های مورد استفاده احترام بگذارند
